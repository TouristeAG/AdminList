# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# java.base/java.io must be open for Kotlin 1.9.x incremental compilation on JDK 17+
# (otherwise: InaccessibleObjectException on FilterOutputStream.out in the Kotlin daemon)
# macOS + JDK 17+: avoids "Failed to exec spawn helper" when Gradle forks AAPT2 and other natives
# (see IDEA-304440). Use "fork" if "vfork" still fails; some JDKs need "posix_spawn".
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8 -Djdk.lang.Process.launchMechanism=fork --add-opens=java.base/java.io=ALL-UNNAMED --add-opens=java.base/java.lang=ALL-UNNAMED
kotlin.daemon.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8 -Djdk.lang.Process.launchMechanism=fork --add-opens=java.base/java.io=ALL-UNNAMED
# Ensures the property is visible as a JVM system property (helps IDE / worker edge cases)
systemProp.jdk.lang.Process.launchMechanism=fork
# org.gradle.java.home can be set locally if needed, but prefer JAVA_HOME env var

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
org.gradle.parallel=true
org.gradle.caching=true
# Single-module app: configure-on-demand can skip work and leave AGP intermediates (e.g. annotationProcessors.json) inconsistent with the task graph.
org.gradle.configureondemand=false

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true

# KSP incremental can hit NoSuchFileException / FileAlreadyExistsException when caches desync
# (e.g. after clean or parallel builds). Full KSP runs are slower but reliable.
ksp.incremental=false
